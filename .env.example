# see https://vercel.com/docs/oidc/aws
AWS_ROLE_ARN=arn:aws:iam::314159265359:role/oidc.vercel

# Route 53 Domains API is only available in so probably just use us-east-1
AWS_REGION=us-east-1

# Minimum seconds between domain-check requests per browser session (default: 5)
THROTTLE_SECONDS=5